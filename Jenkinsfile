pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo Building...'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'echo Docker build...'
            }
        }

        stage('Run Container') {
            steps {
                sh 'echo Running container...'
            }
        }
    }
}
